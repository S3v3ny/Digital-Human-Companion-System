# 基于 AI 大语言模型的情感陪护虚拟数字人系统
## 项目完整方案书

> 第十七届中国大学生服务外包创新创业大赛 · A22 赛题

---

## 目录

1. [项目背景与社会价值](#一项目背景与社会价值)
2. [市场需求分析](#二市场需求分析)
3. [竞品分析与差异化](#三竞品分析与差异化)
4. [系统技术方案](#四系统技术方案)
5. [商业模式](#五商业模式)
6. [项目管理计划](#六项目管理计划)
7. [可行性分析](#七可行性分析)
8. [风险评估与对策](#八风险评估与对策)

---

## 一、项目背景与社会价值

### 1.1 社会问题

中国正经历人类历史上规模最大、速度最快的人口老龄化进程：

- **2023 年**：60 岁以上人口达 **2.97 亿**，占总人口 **21.1%**，正式步入深度老龄化社会
- **2035 年预测**：60 岁以上人口将突破 **4 亿**，占比超过 **30%**
- **空巢/独居老人**：全国超过 **1.18 亿**，占老年人口近 40%
- **心理健康危机**：老年群体抑郁症患病率约 **22.6%**（《柳叶刀》中国研究），焦虑障碍约 **7.9%**，但就诊率不足 **10%**

**"情感孤独"是中国老年群体面临的首要社会问题**，但现有医疗体系、家庭照护体系均无法提供 7×24 小时的情感陪伴与心理干预服务。

### 1.2 项目价值定位

本项目以**情感陪伴**为核心，以**心理干预**为深度，构建面向老年群体的 AI 数字人陪护系统：

| 价值维度 | 具体体现 |
|---|---|
| 情感价值 | 7×24 小时随时在线，不因节假日、情绪波动缺位 |
| 健康价值 | 内嵌 CBT 干预策略，可识别焦虑/抑郁风险并主动引导 |
| 适老价值 | 大字号关怀模式、缓慢语速、亲切称呼，专为老年群体设计 |
| 隐私价值 | 全本地化部署，图像与语音不离开本地设备 |
| 社会价值 | 有效补充专业心理咨询资源短缺，降低家庭照护压力 |

---

## 二、市场需求分析

### 2.1 市场规模

#### 宏观市场（TAM）

```
中国老龄照护产业整体规模
2022年：约 ¥4.2 万亿
2030年预测：≥ ¥10 万亿（年复合增长率约 10%）
```

#### 细分市场（SAM）：AI + 心理健康 + 老年陪护

```
智能养老科技市场
2023年：约 ¥870 亿
2028年预测：约 ¥2,600 亿（CAGR ≈ 25%）
```

其中 **AI 情感陪护数字人**为高速新兴细分，2023 年市场规模约 ¥15 亿，预计 2028 年超 ¥200 亿，五年 CAGR 约 68%，是整个智能养老赛道增速最快的品类。

#### 可服务市场（SOM）

```
目标场景：机构养老（养老院/护理院）+ 社区养老（日照中心）+ 居家养老
全国注册养老机构：约 4.2 万家
配备智能终端的比例（2025 年政策目标）：≥ 30%
初期可触达机构：约 1.3 万家
每家平均 2 套终端，客单价 ¥6,000/年：
SOM ≈ 1.3 万 × 2 × ¥6,000 = ¥1.56 亿/年
```

### 2.2 需求侧验证

#### 政策驱动

| 政策文件 | 核心内容 |
|---|---|
| 《"十四五"国家老龄事业发展规划》 | 明确提出"互联网+养老"，推动智能产品在养老机构普及 |
| 《积极发展老年人数字生活行动方案》 | 鼓励开发适老化智能终端和应用 |
| 《养老机构等级划分与评定》2023修订 | 将"心理关怀服务"列为评星指标 |
| 各地养老数字化采购补贴政策 | 北京、上海、杭州等地对智能养老设备补贴 20~50% |

#### 用户调研摘要

针对 60~80 岁老年用户（n=127，通过问卷星收集）：

- **89%** 表示"儿女不在身边时感到孤独"
- **73%** 希望有人可以随时倾听自己说话，哪怕是 AI
- **61%** 表示愿意尝试语音交互的数字人陪伴产品
- **主要顾虑**：操作复杂（52%）、隐私安全（44%）、"感觉假"（38%）
  - → 本项目针对性设计：关怀模式简化操作、全本地推理保护隐私、情感双模型驱动解决"感觉假"

---

## 三、竞品分析与差异化

### 3.1 竞品全景

| 竞品类型 | 代表产品 | 核心能力 | 主要局限 |
|---|---|---|---|
| **智能音箱** | 天猫精灵、小爱同学、小度 | 语音交互、智能家居控制 | 无面部表情、无情感理解、对话浅层 |
| **陪伴机器人** | LOVOT（日）、PARO（日） | 肢体互动、情感反馈 | 价格高（LOVOT ¥3万+）、无中文支持、无心理干预 |
| **心理健康 APP** | 简单心理、壹心理、MindDoc | 专业内容、在线咨询 | 文字/视频为主、无实时对话、非老年友好 |
| **通用数字人** | 硅基智能、追一科技 | 企业客服、虚拟主播 | B2B 定向、无情感陪护功能、无老年适配 |
| **AI 聊天机器人** | Character.AI、Replika | 开放式对话 | 英文为主、无面部表情、无心理专业知识 |
| **本项目** | Digital-Human-Companion | 见下表 | — |

### 3.2 核心能力对比矩阵

| 能力维度 | 天猫精灵 | 硅基数字人 | 简单心理 APP | **本项目** |
|---|---|---|---|---|
| 中文语音对话 | ✅ | ✅ | ❌ | ✅ |
| 3D 面部表情驱动 | ❌ | ✅ | ❌ | ✅ |
| **说话者口型精准同步** | ❌ | 部分 | ❌ | ✅ Viseme 帧级同步 |
| **倾听者共情微表情** | ❌ | ❌ | ❌ | ✅ SER 驱动 |
| 实时摄像头情绪感知 | ❌ | ❌ | ❌ | ✅ DeepFace |
| CBT 心理干预知识 | ❌ | ❌ | ✅ | ✅ RAG 检索 |
| 全本地推理（隐私） | ❌ | ❌ | ❌ | ✅ |
| 老年关怀模式适配 | 部分 | ❌ | ❌ | ✅ |
| 个性化人设角色 | ❌ | 有限 | ❌ | ✅ 3 角色切换 |
| 打断/续播自然对话 | ❌ | ❌ | ❌ | ✅ Barge-in |
| 提醒日程管理 | ✅ | ❌ | ❌ | ✅ |
| 开源/低成本部署 | ❌ | ❌ | ❌ | ✅ |

### 3.3 四大核心技术壁垒

```
┌─────────────────────────────────────────────────────────────────────┐
│  壁垒一：双模型角色分工架构（行业首创）                               │
│  说话者模型：LLM → TTS → Viseme 口型序列，帧级声画同步               │
│  倾听者模型：用户语音 → SER → ARKit Blendshape，共情微表情           │
│  两个模型并行运行，形成"你说我听都有表情"的自然交互闭环              │
├─────────────────────────────────────────────────────────────────────┤
│  壁垒二：多模态三通道情绪融合                                         │
│  声学通道（SER）+ 视觉通道（DeepFace）+ 文本通道（关键词）            │
│  三路情绪实时融合，形成精准情绪画像，指导数字人回应策略               │
├─────────────────────────────────────────────────────────────────────┤
│  壁垒三：RAG 增强的专业心理干预能力                                   │
│  基于 PsyQA 数据集 + CBT 干预策略知识库                               │
│  每次对话实时检索最匹配的专业干预话术，非通用闲聊                      │
├─────────────────────────────────────────────────────────────────────┤
│  壁垒四：全本地化部署（隐私保护）                                     │
│  ASR（Whisper LoRA）+ SER（HuBERT）+ DeepFace 全部本地运行           │
│  用户语音与视频图像零上传，满足医疗数据安全合规要求                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 四、系统技术方案

### 4.1 五层架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│  Layer 5  交互层   3D TalkingHead + Tailwind 适老化前端           │
├─────────────────────────────────────────────────────────────────┤
│  Layer 4  表达层   Azure TTS + Viseme 口型 + ARKit 表情帧         │
├─────────────────────────────────────────────────────────────────┤
│  Layer 3  生成层   DeepSeek-V3.2 流式 + 文本分块 + TTS 预取       │
├─────────────────────────────────────────────────────────────────┤
│  Layer 2  理解层   Whisper-LoRA + SER + DeepFace + RAG + MCP     │
├─────────────────────────────────────────────────────────────────┤
│  Layer 1  感知层   麦克风 PCM / 摄像头 JPEG / REST /ws             │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 双模型驱动架构详述

#### 说话者模型（声画同步）

```
LLM 流式文本
  → 按标点分块（14~38字/块）
  → Azure Neural TTS（SSML情感风格 + prosody 语速/音调）
  → viseme_received 事件 → Viseme 口型时间轴（22 ID × 毫秒级精度）
  → 前端 TalkingHead 3D 驱动
口型误差：< 40 ms
```

角色 → Azure 声音配置：

| avatarId | 角色 | Azure Voice | 风格 | 语速/音调 |
|---|---|---|---|---|
| 1 | 小丽 | zh-CN-XiaoyiNeural | gentle | +3% / +6% |
| 2 | 老王 | zh-CN-YunzeNeural | documentary-narration | -8% / -8% |
| 3 | 小明 | zh-CN-YunxiNeural | cheerful | +3% / +3% |

#### 倾听者模型（共情微表情）完整流水线

```
用户语音（PCM/WAV）
       │
       ▼
  ① 音频解码 & 重采样 → 16 kHz, float32, mono
       │
       ▼
  ② 语音情感识别（SER）
     模型：xmj2002/hubert-base-ch-speech-emotion-recognition
     （HuBERT-Base，12层Transformer，768维隐状态 + 中文情感语料 fine-tune）
     输出：全标签概率分布
       { "happy": 0.62, "neutral": 0.21, "sad": 0.10, "angry": 0.07, ... }
       │
       ▼
  ③ 情感概率 → ARKit Blendshape 目标权重（加权混合）
     fv_target[idx] = Σ_i prob_i × template_i
     fv_target = clip(fv_target, 0.0, 1.0)
       │
       ▼
  ④ 帧级动态展开（25 FPS，hop=640 samples @ 16kHz）
     - 音频RMS能量调制幅度：energy_factor ∈ [0.65, 1.15]
     - 自然眨眼：正弦曲线，约 4s/次，eyeBlinkLeft[0] / eyeBlinkRight[7]
     - EMA 平滑(α=0.20)：ema = 0.20×target + 0.80×ema
       │
       ▼
  ⑤ 输出帧序列：list[{"emotion": [...×25], "fv": [...×58]}]
```

**ARKit Blendshape 情感模板**（10种，索引对应 ARKit 52维）：

| 情感 | 核心 Blendshape 动作（索引:权重） |
|---|---|
| neutral | mouthSmileL/R(23/24):0.06, browInnerUp(43):0.04 |
| happy | mouthSmileL/R:0.55, cheekSquintL/R(47/48):0.30, eyeSquintL/R(5/12):0.20, jawOpen(17):0.08 |
| sad | mouthFrownL/R(25/26):0.35, browInnerUp:0.40, browDownL/R(41/42):0.20 |
| angry | browDownL/R:0.55, noseSneerL/R(49/50):0.30, mouthPressL/R(35/36):0.25 |
| surprise | eyeWideL/R(6/13):0.60, browOuterUpL/R(44/45):0.50, jawOpen:0.35 |
| fear | eyeWideL/R:0.45, browOuterUpL/R:0.35, mouthStretchL/R(29/30):0.20 |
| disgust | noseSneerL/R:0.45, browDownL/R:0.30, mouthFrownL/R:0.25 |

**用户体验效果**：当用户诉说悲伤时，数字人眉头微蹙、嘴角轻压，眼部温和注视，形成自然的"我在认真听你说"的共情状态，而非呆滞等待。

#### 三通道情绪感知融合

| 通道 | 触发条件 | 技术方案 | 用途 |
|---|---|---|---|
| 摄像头通道 | 每 1 秒限频 | DeepFace → `dominant_emotion` | 注入 LLM system prompt，影响回复语气 |
| 语音通道 | 用户音频到达时 | SER（HuBERT）→ 主情感标签 | 驱动数字人聆听时的共情表情帧序列 |
| 文本通道 | LLM 每个输出 chunk | 关键词匹配 `_EMOTION_KW` | 驱动数字人说话时的情绪 blendshape |

文本通道关键词映射（`server.py:286`）：
- "开心/太棒了/恭喜" → `happy`；"难过/委屈/心疼" → `sad`
- "真的吗/居然/哇" → `surprise`；"理解你/陪着你/慢慢来" → `caring`

### 4.3 五层架构接口协议与数据格式

> 本节按五层架构逐层列出所有对外暴露的接口与内部模块调用契约，包含字段类型、约束、错误码及典型数据示例。

---

#### Layer 1 — 感知层

##### 1.1 WebSocket 实时通信接口

**连接地址**：`ws://<host>:8000/ws/chat`

**传输格式**：全部为 JSON 文本帧（UTF-8），服务端与客户端均通过 `{"type": "..."}` 区分消息种类。

**连接生命周期**：
```
客户端连接
  └─ 发送 init（必须第一条发送，建立会话上下文）
       └─ 服务端回推 reminder_list（恢复本次连接前已有的提醒）
            └─ 正常对话循环（audio/message/frame 等）
                 └─ 客户端断开（WebSocketDisconnect）
                      └─ 服务端清理所有后台 Task
```

---

**上行消息（客户端 → 服务端）**

消息类型汇总表：

| type | 触发时机 | 必填字段 | 可选字段 |
|---|---|---|---|
| `init` | 连接建立后第一条 | `sessionId` | `avatarId`, `userName` |
| `new_session` / `switch_session` | 切换历史会话 | `sessionId` | `userName` |
| `audio` | 用户录音结束 | `data` | — |
| `message` | 用户键盘输入 | `content` | `userName` |
| `text` | 同 `message`（别名） | `content` | `userName` |
| `barge_in_commit` | 前端 VAD 切片后提交 | `content` | `userName` |
| `frame` | 摄像头帧（1 s 限频） | `data` | — |
| `barge_in_start` | VAD 检测到说话开始 | — | — |
| `ping` | 心跳保活（建议 30 s/次） | — | — |

---

**`init` — 会话初始化**

```jsonc
{
  "type": "init",
  "sessionId": "user_abc",   // string，必填；标识用户会话，历史记忆按此 key 存取
  "avatarId":  1,            // int，可选，默认 1；1=小丽 2=老王 3=小明
  "userName":  "张爷爷"       // string，可选；数字人在对话中的称呼词
}
```

服务端行为：设置当前连接的 `session_id`、`avatar_id`、`user_name`；立即下推 `reminder_list` 消息。

---

**`audio` — 语音音频**

```jsonc
// 方式 A：PCM Int16 数组（16 kHz, mono，由前端 AudioWorklet 直接采集）
{
  "type": "audio",
  "data": [120, -340, 890, ...]   // int16[] 原始 PCM 采样点
}

// 方式 B：base64 编码的 WAV/MP3（含文件头）
{
  "type": "audio",
  "data": "data:audio/wav;base64,UklGRi..."
}
```

服务端行为：
1. 若当前有正在播报的回复，先触发 `audio_barge_in` 打断
2. ASR（Whisper-LoRA）与 SER（HuBERT）**并行**推理
3. SER 完成后即刻下推 `user_emotion`，给前端驱动共情表情
4. ASR 完成后进入 `handle_user_text` 对话流程

方式 A 服务端自动封包为 16 kHz / mono / Int16 WAV，无需客户端额外处理。

---

**`message` / `text` / `barge_in_commit` — 文本输入**

```jsonc
{
  "type": "message",          // 或 "text" 或 "barge_in_commit"，三者等价
  "content": "最近总是睡不着，怎么办",
  "userName": "张爷爷"         // 可选，覆盖当前连接的 user_name
}
```

`content` 字段为空（空字符串 / 全空白）时服务端静默丢弃，不产生任何回复。

---

**`frame` — 摄像头帧**

```jsonc
{
  "type": "frame",
  "data": "data:image/jpeg;base64,/9j/4AAQ..."   // JPEG DataURL 或纯 base64
}
```

限频逻辑（`server.py:93`）：`EMOTION_INTERVAL = 1.0 s`；连续帧到达时仅处理第一帧，并设置 `emotion_busy` 锁防止线程池被打满。DeepFace CPU 推理耗时约 < 200 ms。

---

**`barge_in_start` — 打断预告**

```jsonc
{ "type": "barge_in_start" }
```

前端 VAD 检测到用户开口时即刻发送（早于音频数据到达）。服务端立即取消当前播报任务并下推 `stop_output`，使数字人停止说话进入倾听状态，减少感知延迟。

---

**`ping` — 心跳**

```jsonc
{ "type": "ping" }
```

服务端立即回复 `{"type": "pong"}`，用于检测连接活性。

---

##### 1.2 REST HTTP 接口

服务运行于 `http://<host>:8000`，提供以下 HTTP 接口：

---

**`POST /asr` — 官方评测 ASR 打分接口**

```
POST /asr
Content-Type: application/octet-stream
Body: <音频文件二进制，支持 mp3/wav/任意格式，≤ 10 MB>
```

响应：

| HTTP 状态码 | Body | 说明 |
|---|---|---|
| 200 | `{"result": "识别出的中文文本"}` | 正常识别 |
| 200 | `{"result": ""}` | 音频为空，返回空字符串 |
| 200 | `{"result": "识别错误:<异常信息>"}` | 模型内部异常，降级返回错误描述 |
| 413 | `{"result": "文件过大"}` | 超过 10 MB 限制 |

此接口供官方评测脚本调用，与 WebSocket `audio` 使用同一个 `asr_engine` 实例，无需重复加载模型。推理在线程池执行，不阻塞 WebSocket 事件循环。

---

**`GET /api/weather?city=<城市名>` — 实时天气**

```
GET /api/weather?city=北京
```

响应（200）：

```jsonc
{
  "ok": true,
  "city": "北京",
  "temp": 23,           // 整数，℃
  "feels_like": 21,
  "text": "多云",
  "wind_dir": "东南风",
  "wind_scale": "3",
  "wind_speed": 4.2,
  "humidity": "65",
  "raw_text": "<MCP 原始返回>"
}
// 查询失败时：
{ "ok": false, "error": "天气 MCP 超时" }
```

数据源：ModelScope 托管的天气 MCP SSE 服务（`WEATHER_MCP_URL` 环境变量可覆盖）。`city` 为空时使用 `DEFAULT_CITY`（默认 `北京`）。

---

**`GET /api/calendar` — 本地日历**

```
GET /api/calendar
```

响应（200）：

```jsonc
{
  "ok": true,
  "year": 2026, "month": 5, "day": 22,
  "weekday": "星期五",
  "time": "14:30",
  "lunar": "农历四月廿五"   // 可选，依赖 zhdate 包；不可用时字段缺失
}
```

完全本地计算，无网络依赖。

---

**`GET /` — 前端入口**

返回 `./public/index.html`，静态资源挂载于 `/static/`（对应 `./public/` 目录）。

---

#### Layer 2 — 理解层

##### 2.1 ASR 模块（`asr.py`）

```python
class CustomASR:
    def __init__(
        self,
        base_model_path: str = "./models/whisper-small-model",
        lora_model_path: str = "./whisper-final-model",
    ): ...

    def speech_to_text(self, audio_bytes: bytes | None = None) -> str:
        """
        两种调用模式：
          - audio_bytes 不为 None：WebSocket/REST 模式，接受 WAV/PCM 字节流
          - audio_bytes 为 None  ：麦克风模式（独立测试用）

        返回值：UTF-8 中文字符串；异常时返回 "识别过程出错: <reason>" 而非抛出异常。

        模型规格：
          基础模型  openai/whisper-small（244 M params）
          适配器    PEFT LoRA，rank=8，alpha=32，target_modules=["q_proj","v_proj"]
          推理参数  language="chinese", max_new_tokens=128
          设备      device_map="auto"（有 GPU 用 GPU，否则 CPU）
        """
```

**输入音频规格约束**：

| 参数 | 要求 |
|---|---|
| 采样率 | 16 kHz（内部自动重采样） |
| 声道 | mono（内部自动降混） |
| 格式 | WAV/PCM Int16 均可；方式 A PCM 数组服务端封包为 WAV 后传入 |
| 时长 | 建议 ≤ 30 s（超长音频 max_new_tokens=128 可能截断） |

**错误降级**：所有异常均被捕获，返回字符串形式错误描述，不中断上层流程。

---

##### 2.2 SER 模块（`faceformer_adapter.py`）

```python
def audio_bytes_to_user_emotion(audio_bytes: bytes) -> str | None:
    """
    返回值：归一化主情感标签（str）或 None（推理失败时）
    可能的标签：angry / disgust / fear / happy / neutral / sad / surprise
    内部流程：
      1. 解码音频 → 重采样至 16 kHz float32 mono
      2. HuBERT-Base-ch 推理 → 7 类 softmax 概率
      3. argmax → 主情感标签
    """

def audio_bytes_to_listener_reaction(audio_bytes: bytes) -> list[dict]:
    """
    返回值：25 FPS 帧序列，帧数 N = ceil(audio_duration_sec × 25)
    每帧结构：
      {
        "emotion": list[float] × 25,  # [0:15] 动作单元 AU, [15:17] Valence/Arousal, [17:25] EXP概率
        "fv":      list[float] × 58,  # [0:52] ARKit 52维 Blendshape, [52:55] 头部姿态, [55:58] 平移
      }
    失败回退：调用 _fallback(audio_bytes)，以 RMS 能量驱动下巴开合（jawOpen）+ 自然眨眼，
              保证帧序列始终非空，不中断前端渲染循环。
    """
```

**情感→Blendshape 映射核心参数**（`faceformer_adapter.py`）：

| 参数 | 值 | 说明 |
|---|---|---|
| 帧率 | 25 FPS | hop = 640 samples @ 16kHz |
| EMA 平滑系数 α | 0.20 | ema = 0.20×target + 0.80×ema，防止表情跳变 |
| 能量调制范围 | [0.65, 1.15] | 按 RMS 能量缩放幅度，说话越响表情越明显 |
| 眨眼周期 | ~4 s/次 | 正弦曲线驱动 eyeBlinkLeft[0] / eyeBlinkRight[7] |

---

##### 2.3 视觉情绪识别（`emotion.py`）

```python
def get_face_emotion(img_bytes: bytes) -> str:
    """
    输入：JPEG/PNG 图片字节（来自 WebSocket frame 消息）
    输出：dominant_emotion 字符串（DeepFace 7 类标签）
    调用方式：线程池执行（run_in_executor），不阻塞事件循环
    CPU 推理耗时：< 200 ms
    限频：每 1 s 至多处理 1 帧（EMOTION_INTERVAL = 1.0），emotion_busy 互斥锁保证不并发
    用途：注入 LLM system_prompt 中的 {emotion} 占位符，影响数字人回复语气
    """
```

---

##### 2.4 RAG 检索模块（`llm.py`）

```python
async def get_rag_context(user_text: str, top_k: int = 2) -> str:
    """
    异步接口，内部通过 run_in_executor 调用同步检索，不阻塞 LLM 流式输出。

    返回值：格式化字符串，形如：
      "参考干预案例 1:\n<文档片段>\n\n参考干预案例 2:\n<文档片段>"
    检索失败时返回 ""（空字符串），LLM 退化为纯靠自身知识回复。
    """
```

**向量检索参数**：

| 参数 | 值 |
|---|---|
| 向量模型 | BAAI/bge-small-zh-v1.5（384 维，本地加载） |
| 向量数据库 | ChromaDB PersistentClient，路径 `./vector_db` |
| Collection | `psy_cbt_knowledge` |
| 数据集来源 | thu-coai/PsyQA + CBT 干预策略标注 |
| top_k | 2（每次对话检索最相关 2 条） |
| 检索耗时 | < 50 ms |

ChromaDB 查询协议：

```python
query_embedding = embedder.encode([user_text]).tolist()  # list[list[float384]]
results = psy_collection.query(
    query_embeddings=query_embedding,
    n_results=2,
)
# results["documents"][0]: list[str]，长度 = n_results
```

---

##### 2.5 工具调用路由（`tools.py`）

```python
def detect_tool(user_text: str) -> str | None:
    """
    关键词路由，返回工具名或 None（不命中则不调用任何工具，LLM 靠自身知识回答）。
    当前支持的工具：
      "weather" — 触发关键词：天气 / 温度 / 下雨 / 出门冷 / 穿多少 / 几度
    """

async def call_tool(tool_name: str, user_text: str = "") -> str:
    """
    返回值：格式化工具上下文字符串，注入 LLM system_prompt。
    天气结果示例：
      "【实时天气】\n今天北京天气多云，气温23度。风向东南风，3级风。湿度65%。\n【念稿要求】..."
    调用失败时返回 "工具调用失败：<reason>"，LLM 据此告知用户查询失败。
    """
```

天气 MCP 调用参数：

| 参数 | 值 |
|---|---|
| 协议 | SSE（Server-Sent Events） |
| 服务地址 | `WEATHER_MCP_URL` 环境变量，默认 ModelScope 托管地址 |
| 超时 | 25 s（`_call_sse`），列工具 10 s |
| 参数键自动探测 | 依次尝试 `city` / `location` / `city_name` / `place` / `query` |

---

#### Layer 3 — 生成层

##### 3.1 LLM 流式对话 API

**调用端点**：`POST https://api.siliconflow.cn/v1/chat/completions`（OpenAI 兼容接口）

**请求头**：
```
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

**主对话请求体**：

```jsonc
{
  "model": "deepseek-ai/DeepSeek-V3.2",
  "messages": [
    {
      "role": "system",
      "content": "<动态构建的 system_prompt，含角色人设 + 情绪感知 + RAG 上下文 + 工具结果>"
    },
    // 最近 MAX_HISTORY_MESSAGES=6 条历史（交替 user/assistant）
    { "role": "user",      "content": "我最近睡不着" },
    { "role": "assistant", "content": "我能感受到..." },
    { "role": "user",      "content": "昨晚又失眠了" }   // 当前轮
  ],
  "temperature": 0.7,
  "max_tokens": 2048,
  "stream": true,
  "timeout": 60         // aiohttp session 超时（秒）
}
```

**SSE 流式响应格式**：
```
data: {"choices":[{"delta":{"content":"我"},"index":0}]}
data: {"choices":[{"delta":{"content":"能"},"index":0}]}
...
data: [DONE]
```

**打断分类子调用**（用户插话时触发，`llm.py:classify_interrupt_intent`）：

```jsonc
// 先走本地规则（零延迟）：
//   含"停一下/别说了/打住"等 → interrupt
//   ≤6字且在 ACK 词表中 → ack
//   含"其实/补充/刚才" → supplement
//   其余 → new_topic（触发 API 分类）

// 本地规则未能确定时，发起 API 调用：
{
  "model": "deepseek-ai/DeepSeek-V3.2",
  "messages": [
    {"role": "system", "content": "你是一个严格的分类器。"},
    {"role": "user",   "content": "<分类 prompt，含已说内容/未说内容/用户插话>"}
  ],
  "temperature": 0.1,
  "max_tokens": 16,
  "stream": false,
  "timeout": 15
}
// 返回值：interrupt | ack | supplement（小写，无其他内容）
```

**补充回复构建子调用**（打断类型为 `supplement` 时，`llm.py:build_followup_reply`）：

```jsonc
{
  "model": "deepseek-ai/DeepSeek-V3.2",
  "messages": [
    {"role": "system", "content": "你是一个口语化、自然、有耐心的中文语音助手。"},
    {"role": "user",   "content": "<含已说内容/未说内容/用户插话的融合 prompt>"}
  ],
  "temperature": 0.7,
  "max_tokens": 256,
  "stream": false,
  "timeout": 20
}
// 要求：50~120 字，先回应插话，自然融入未说完的内容
```

---

##### 3.2 文本分块策略（`server.py:319~384`）

LLM 字符流经 `should_emit_chunk` + `split_next_chunk` 实时切分：

| 优先级 | 切分触发条件 | 最小累积长度 | 说明 |
|---|---|---|---|
| 1 | 句末标点 `。！？!?` | ≥ 4 字符 | `MIN_SENTENCE_LENGTH`，防空句 |
| 2 | 句中标点 `，,` | ≥ 14 字符 | `MIN_CHUNK_LENGTH` |
| 3 | 强制截断 | ≥ 38 字符 | `MAX_CHUNK_LENGTH`，兜底 |

**括号防切逻辑**（`has_unclosed_bracket`）：检测缓冲区是否有未闭合的 `（）/()`；有则暂停切分，等收到闭合括号后再切，防止 LLM 旁白指令（如"（语气转为关切）"）被截断后漏过 `sanitize_chunk` 清洗。双重保险：缓冲超过 `MAX_CHUNK_LENGTH × 2` 时强制切，防 LLM 漏写闭合符号卡死。

**`sanitize_chunk` 清洗规则**（顺序执行）：

| 规则 | 正则 | 用途 |
|---|---|---|
| 去元标签 | `【[^】]{0,30}】` | 清除 LLM 偶尔泄露的【实时天气】等 |
| 去中文旁白 | `（[^）]{1,40}）` | 清除（语气转为关切）等 |
| 去英文旁白 | `\([^)\d]{2,40}\)` | 清除 (softly) 等；保留 (30%) 数字括号 |
| 去 Markdown | `\*{1,3}` / `^#{1,6}` / `` `{1,3} `` | 防止加粗/标题/代码被 TTS 念出 |
| 去多余空格 | `[ \t]{2,}` | 归一化空白 |

---

##### 3.3 TTS 预取队列与排序保证（`server.py:407~487`）

```
LLM 字符流
    │ handle_text_piece（每次字符流事件）
    ▼
chunk_queue: asyncio.Queue[str | sentinel]
    │
    ▼ tts_worker Task（并发预取，与 LLM 生成并行）
    ├── schedule_chunk(clean_text)
    │       → 创建 asyncio.Task（synthesize_chunk_pipeline）
    │       → task.seq = 单调递增序号
    │       → task.emotion = detect_text_emotion(clean_text)
    │       → 加入 pending_tasks: set[Task]
    │
    ├── flush_ready(force=False)
    │       → 取 pending_tasks 中 seq 最小的任务
    │       → force=False 时只 flush 已完成的（背压控制）
    │       → force=True  时 await 等待（LLM Done 后排空队列）
    │       → 按 seq 顺序发送，保证音频不乱序
    │
    └── 预取窗口 TTS_PREFETCH_CHUNKS = 2
            → pending_tasks.size >= 2 时先 flush 再继续 schedule
```

**TTS 降级路径**：`synthesize_chunk_pipeline` 抛出 `TTSGenerationError` 时，退化为发送不含 `audio` 字段的 `assistant_chunk`，`fallback: "text_only"` 标记，前端仅显示文字气泡，数字人静默不动嘴，对话不中断。

---

##### 3.4 Azure TTS SSML 格式（`tts.py`）

```xml
<speak version="1.0"
       xmlns="http://www.w3.org/2001/10/synthesis"
       xmlns:mstts="http://www.w3.org/2001/mstts"
       xml:lang="zh-CN">
  <voice name="{voice}">
    <mstts:express-as style="{style}" styledegree="1.5">
      <prosody rate="{rate}" pitch="{pitch}">
        {XML转义后的文本}
      </prosody>
    </mstts:express-as>
  </voice>
</speak>
```

三角色 TTS 配置（`tts.py:ROLE_CONFIG`）：

| avatarId | 角色 | voice | style | rate | pitch |
|---|---|---|---|---|---|
| 1 | 小丽 | `zh-CN-XiaoyiNeural` | `gentle` | `+3%` | `+6%` |
| 2 | 老王 | `zh-CN-YunzeNeural` | `documentary-narration` | `-8%` | `-8%` |
| 3 | 小明 | `zh-CN-YunxiNeural` | `cheerful` | `+3%` | `+3%` |

**输出格式**：`Audio16Khz32KBitRateMonoMp3`（16 kHz, 32 kbps, mono MP3）

**超时与错误处理**：
- 合成超时：30 s（`asyncio.wait_for`），超时抛出 `TTSGenerationError("Azure TTS 合成超时（30s）")`
- Azure 返回空音频：抛出 `TTSGenerationError("Azure TTS 返回空音频")`
- Azure SDK 取消（网络断开等）：`CancellationDetails` 含 `error_code` + `error_details`，完整信息记录日志

**XML 安全转义**（防注入破坏 SSML）：`& → &amp;`，`< → &lt;`，`> → &gt;`，`" → &quot;`

---

#### Layer 4 — 表达层

##### 4.1 Viseme 口型时间轴格式

由 Azure SDK `viseme_received` 事件回调产生，单位换算：`audio_offset`（100 ns）→ ms：

```python
visemes: list[dict] = [
    {
        "time_ms":   int(evt.audio_offset) // 10_000,  # 相对音频起始的毫秒偏移
        "viseme_id": int(evt.viseme_id),               # SAPI Viseme ID，范围 0~21
    },
    ...
]
```

**SAPI Viseme ID 映射**（22 个音素类别）：

| ID | 对应音素 | ID | 对应音素 |
|---|---|---|---|
| 0 | 静音/闭嘴 | 11 | aw（如"啊") |
| 1 | æ/ɑ | 12 | oy |
| 2 | ɑ | 13 | uw（如"乌"） |
| 3 | ʌ/ɛ | 14 | uw |
| 4 | ɛ/ʌ | 15 | oʊ（如"哦"） |
| 5 | ɜ | 16 | aw |
| 6 | ɪ/ɛ | 17 | b/p/m（双唇音） |
| 7 | w/ʊ | 18 | f/v |
| 8 | o | 19 | θ/ð |
| 9 | aɪ | 20 | d/t/n/l/s/z |
| 10 | eɪ | 21 | g/k/ŋ |

前端 TalkingHead 3D 根据 `time_ms` + 当前音频播放进度，线性插值驱动对应 Blendshape，口型误差 < 40 ms。`visemes` 为空列表时 `assistant_chunk` 不携带该字段（减少带宽）。

---

##### 4.2 情感帧序列格式（倾听者共情反应）

由 `faceformer_adapter.audio_bytes_to_listener_reaction` 输出：

```python
frames: list[dict] = [
    {
        # ─── emotion 向量（25 维）───────────────────────────────────────────
        "emotion": [
            # [0:15]  动作单元 AU（Action Units）连续值，范围 [0.0, 1.0]
            float,  # AU01 内眉上扬
            float,  # AU02 外眉上扬
            float,  # AU04 皱眉
            float,  # AU05 上眼睑上提
            float,  # AU06 颧骨收缩
            float,  # AU07 眼睑收紧
            float,  # AU09 鼻翼皱起
            float,  # AU10 上唇提起
            float,  # AU12 嘴角上扬（笑肌）
            float,  # AU14 酒窝
            float,  # AU15 嘴角下拉
            float,  # AU17 下颌收紧
            float,  # AU20 嘴角向外拉
            float,  # AU23 嘴唇收紧
            float,  # AU25 嘴唇分开
            # [15:17] Valence / Arousal（效价/唤醒度）连续值，范围 [-1.0, 1.0]
            float,  # Valence（负→正）
            float,  # Arousal（平静→激动）
            # [17:25] 8类情感概率（softmax），范围 [0.0, 1.0]，和为 1.0
            float,  # P(neutral)
            float,  # P(happy)
            float,  # P(sad)
            float,  # P(surprise)
            float,  # P(fear)
            float,  # P(disgust)
            float,  # P(angry)
            float,  # P(contempt)
        ],
        # ─── fv（Face Vector，58 维）──────────────────────────────────────
        "fv": [
            # [0:52]  ARKit 标准 52 维 Blendshape，范围 [0.0, 1.0]
            # 索引与 ARKit BlendShapeLocation 一一对应（eyeBlinkLeft=0, ...）
            float * 52,
            # [52:55] 头部姿态（欧拉角，单位：弧度）
            float,  # pitch（俯仰，低头/抬头）
            float,  # yaw（偏航，左转/右转）
            float,  # roll（侧倾）
            # [55:58] 头部平移（归一化，相对图像中心）
            float,  # tx
            float,  # ty
            float,  # tz
        ],
    },
    # ...共 N 帧，N = ceil(audio_duration_sec × 25)
]
```

前端以 40 ms 间隔（25 FPS）逐帧取出并设置 TalkingHead 的 `morphTargetInfluences`，驱动倾听者共情微表情动画。

---

#### Layer 5 — 交互层（WebSocket 下行消息）

##### 5.1 完整下行消息类型总表

| type | 触发时机 | 关键字段 |
|---|---|---|
| `turn_start` | 服务端开始处理用户输入 | `responseId`, `userText` |
| `assistant_text_delta` | LLM 每切出一个 chunk | `responseId`, `delta`, `fullText` |
| `assistant_chunk` | 对应 chunk 的 TTS 合成完毕 | `responseId`, `seq`, `text`, `audio`, `visemes`, `emotion` |
| `turn_end` | 整轮回复发送完毕 | `responseId`, `fullText` |
| `turn_interrupted` | 回复被用户打断 | `responseId`, `spokenText`, `pendingText` |
| `stop_output` | 服务端主动停止当前播报 | `responseId`, `reason`, `stopAudio`, `stopBlendshape`, `enterListening` |
| `listen_state` | 数字人进入倾听状态 | `responseId`, `state`, `microActions` |
| `interrupt_classified` | 打断意图分类完成 | `responseId`, `intent` |
| `user_emotion` | SER 识别到用户情绪 | `emotion` |
| `reminder_added` | 用户成功设置提醒 | `reminder` |
| `reminder_fired` | 提醒到期触发 | `reminder` |
| `reminder_list` | `init` 时恢复提醒列表 | `reminders` |
| `pong` | 心跳响应 | — |
| `error` | 服务端处理异常 | `message` |

---

##### 5.2 对话流消息详解

**完整一轮正常对话的消息序列**：
```
turn_start
  → assistant_text_delta × N  （与 TTS 合成并发，气泡先行显示）
  → assistant_chunk × N        （TTS 完成后携带音频）
  → turn_end
```

```jsonc
// ① turn_start — 服务端开始处理，可用于前端显示"正在思考..."
{
  "type": "turn_start",
  "responseId": 42,              // int，本轮回复唯一 ID（单调递增）
  "userText": "最近总是睡不着"    // string，服务端确认收到的用户文本
}

// ② assistant_text_delta — 文本流（先于音频到达，驱动聊天气泡逐字显示）
{
  "type": "assistant_text_delta",
  "responseId": 42,
  "delta": "我能感受到",          // string，本次新增片段
  "fullText": "我能感受到"        // string，截至本帧的累积全文
}

// ③ assistant_chunk — 完整音频分片（含音频+口型+情绪）
{
  "type": "assistant_chunk",
  "responseId": 42,
  "seq": 0,                      // int，从 0 递增，前端按 seq 排序播放
  "text": "我能感受到你现在有些不安。",  // string，本分片文本
  "fullText": "我能感受到你现在有些不安。",  // string，累积全文
  "isFinalChunk": false,         // bool，末尾分片为 true（目前固定 false，turn_end 标识结束）
  "audio": "<base64 MP3>",       // string，Audio16Khz32KBitRateMonoMp3；TTS 失败时字段缺失
  "visemes": [                   // list，Viseme 口型序列；为空时不发送此字段
    {"time_ms": 0, "viseme_id": 0},
    {"time_ms": 80, "viseme_id": 11},
    ...
  ],
  "emotion": "caring",           // string，文本情感标签；neutral 时不发送此字段
  "fallback": "text_only"        // string，仅 TTS 失败降级时存在；前端据此静音不动嘴
}

// ④ turn_end — 本轮回复完全结束
{
  "type": "turn_end",
  "responseId": 42,
  "fullText": "我能感受到你现在有些不安。慢慢来，你愿意跟我说说是什么让你难以入眠吗？"
}
```

---

##### 5.3 打断与控制消息详解

**被打断时的消息序列**：
```
（用户发送 barge_in_start 或 audio/message）
  → stop_output          （通知前端立即停止音频播放和 Blendshape 动画）
  → listen_state         （数字人切换为倾听微表情：目光接触 + 轻点头）
  → interrupt_classified （告知前端打断意图，影响下一轮回复策略）
  → turn_interrupted     （标记被打断的回复已终止，携带已说/未说文本）
  → turn_start           （新一轮立即开始）
```

```jsonc
// stop_output — 停止当前播报
{
  "type": "stop_output",
  "responseId": 43,              // int，新分配的 responseId（比被打断的 +1）
  "reason": "barge_in",          // string：barge_in | barge_in_start | audio_barge_in
  "stopAudio": true,             // bool，前端停止音频播放
  "stopBlendshape": true,        // bool，前端停止 Blendshape 动画
  "enterListening": true         // bool，前端切换数字人为倾听姿态
}

// listen_state — 数字人倾听微表情
{
  "type": "listen_state",
  "state": "listening",          // string，当前状态
  "responseId": 43,
  "microActions": ["eye_contact", "nod"]  // list[string]，前端执行的微动作
}

// interrupt_classified — 打断意图分类
{
  "type": "interrupt_classified",
  "intent": "supplement",        // string：interrupt | ack | supplement
  "responseId": 43
  // interrupt：用户明确要换话题/纠错
  // ack：用户只是"嗯哦好"等短回应
  // supplement：用户在补充信息，期望结合新信息继续
}

// turn_interrupted — 回复被中断（当前回复的终态）
{
  "type": "turn_interrupted",
  "responseId": 42,              // int，被打断的那轮 responseId
  "spokenText": "我能感受到你",   // string，已经播放出来的部分
  "pendingText": "现在有些不安。" // string，已 TTS 合成但尚未播完的部分
}
```

---

##### 5.4 情绪与状态消息

```jsonc
// user_emotion — 用户情绪（SER 推理结果，驱动倾听者共情表情）
{
  "type": "user_emotion",
  "emotion": "sad"   // string：angry | disgust | fear | happy | neutral | sad | surprise
}

// pong — 心跳响应
{ "type": "pong" }

// error — 服务端处理异常
{
  "type": "error",
  "message": "语音识别失败"   // string，面向用户的简短描述
}
```

---

##### 5.5 提醒功能消息

```jsonc
// reminder_added — 成功登记提醒（用户说"明天上午九点提醒我吃药"后立即下推）
{
  "type": "reminder_added",
  "reminder": {
    "id":       "a3f8c1b2",           // string，8位 hex UUID 前缀
    "whenTs":   1748480400.0,          // float，Unix 时间戳（秒）
    "whenStr":  "明天 09:00",          // string，人类可读时间（今天/明天/后天/MM月DD日 HH:MM）
    "content":  "吃药",               // string，提醒事项（已清洗时间噪声）
    "fired":    false                  // bool，是否已触发
  }
}

// reminder_fired — 提醒到期（扫描间隔 5 s，到期后主动推送并让数字人播报）
{
  "type": "reminder_fired",
  "reminder": { ...同上结构，fired: true... }
}

// reminder_list — 连接建立时（init 后）下推当前活跃提醒列表
{
  "type": "reminder_list",
  "reminders": [
    { "id": "a3f8c1b2", "whenTs": 1748480400.0, "whenStr": "明天 09:00", "content": "吃药", "fired": false },
    ...
  ]
}
```

提醒时间解析能力（`reminders.py`）：

| 表达形式 | 示例 |
|---|---|
| 相对延迟 | 3分钟后、5秒后、两小时后 |
| 今日时刻 | 下午三点、晚上八点半、上午十点 |
| 次日时刻 | 明天早上九点、明天下午两点 |
| 后天时刻 | 后天中午十二点 |

已过时刻（未指定"今天"且时间已过）自动顺延到明天。

---

##### 5.6 会话记忆持久化（`session_memory.json`）

服务进程内存中维护 `session_history: dict[str, list]`，每次收到完整 assistant 回复后写盘：

```jsonc
{
  "user_abc": [
    {"role": "user",      "content": "最近睡不着"},
    {"role": "assistant", "content": "我能感受到，慢慢来..."},
    {"role": "user",      "content": "昨晚又失眠了"},
    {"role": "assistant", "content": "昨晚也没睡好..."}
    // 滑动窗口，最多保留最近 MAX_HISTORY_MESSAGES = 6 条
  ],
  "user_xyz": [ ... ]
}
```

并发写保护：`memory_lock: asyncio.Lock()`，`append_user_message` 与 `commit_assistant_message` 均在锁内操作，防止多用户并发时写文件竞争。

被打断的回复（`response_state.interrupted = True`）不调用 `commit_assistant_message`，不记入历史，避免污染上下文。

### 4.4 关键性能指标

| 指标 | 实测值 | 对比基线 |
|---|---|---|
| 端到端延迟（用户说完→首音节） | 0.8~1.2 s | 业界水平 2~5 s |
| 中文 ASR 字错率（CER） | < 8% | Whisper-Small 基础模型 ~18% |
| SER 情感识别准确率（6类） | ~76% | 业界开源模型基准 ~72% |
| 打断恢复时间 | < 300 ms | 通用方案 ~1 s |
| 25维评测输出格式 | [N,K,T,25] 通过 | 官方 eval_emotion_metrics.py 验证 |

### 4.5 适老化设计细节

| 设计点 | 实现方式 | 解决的老年用户痛点 |
|---|---|---|
| 关怀模式 | 根字号放大 25%，所有 rem 单位等比缩放 | 视力下降，看不清 |
| 缓慢语速 | Azure TTS `rate="-8%"` + 老王角色专属配置 | 语速快，跟不上 |
| 亲切称呼 | 用户名存 localStorage，数字人自然叫出 | 感觉被重视，不孤独 |
| 打断宽容 | 任何时候说话即打断，无需按键 | 操作复杂，不会用按钮 |
| 提醒功能 | 自然语言设置："明天上午九点提醒我吃药" | 记性不好，依赖提醒 |
| 多角色选择 | 小丽/老王/小明，风格各异可切换 | 不同老人偏好不同陪伴风格 |

---

## 五、商业模式

### 5.1 商业模式画布

#### 客户细分（Customer Segments）

```
B 端（主要付费方）：
  ├── 机构养老：养老院、护理院、老年公寓（全国约 4.2 万家）
  ├── 社区养老：街道日照中心、社区服务中心（全国约 2.5 万个）
  └── 医疗机构：精神科门诊、老年病科（配套心理辅助工具）

G 端（政府采购）：
  └── 民政局、卫健委"智慧养老"专项采购

C 端（辅助渠道）：
  └── 子女为空巢父母购买居家版套装
```

#### 价值主张（Value Proposition）

```
对机构养老客户：
  "一个 AI 数字人替代部分心理关怀岗位成本，7×24 小时在线，每月仅需 ¥500 起"

对政府采购方：
  "满足养老机构星级评定'心理关怀服务'指标，可申请数字化养老补贴"

对老人家属：
  "随时了解父母情绪状态，不在身边也能给父母留下陪伴"
```

#### 收入来源（Revenue Streams）

| 产品/服务 | 定价模式 | 客单价 | 目标客群 |
|---|---|---|---|
| **机构版 SaaS 订阅** | 按终端/月 | ¥500~1,200/终端/月 | 养老院、日照中心 |
| **硬件套装**（平板+系统） | 一次性 + 年维护 | ¥3,800/套 + ¥600/年 | 居家养老、小型机构 |
| **定制开发服务** | 项目制 | ¥5~30 万/项目 | 大型连锁养老集团 |
| **政府采购包** | 框架协议 | 按机构数量阶梯定价 | 民政局、街道办 |
| **API 授权** | 调用量计费 | ¥0.05/次对话 | 第三方养老 APP 集成 |

#### 成本结构（Cost Structure）

| 成本类型 | 占比 | 说明 |
|---|---|---|
| API 调用成本（LLM + TTS） | ~15% | 按用量，随规模摊薄 |
| 云服务器/CDN | ~10% | 管理端 + 数据看板 |
| 研发人员 | ~45% | 核心竞争力，持续迭代 |
| 销售与市场 | ~20% | 参展、渠道佣金 |
| 运营与客服 | ~10% | 机构培训、技术支持 |

#### 盈利预测

```
第一年（拓展期）：
  目标签约机构 50 家，平均 2 终端/机构
  SaaS 收入：50 × 2 × ¥800/月 × 12 = ¥96 万
  硬件收入：100 套 × ¥3,800 = ¥38 万
  合计营收：约 ¥134 万，接近盈亏平衡

第二年（规模化）：
  目标签约机构 300 家，政府采购框架落地
  预计营收：¥800~1,000 万，净利率约 20~30%

第三年（生态化）：
  API 授权 + 定制服务规模化
  预计营收：¥3,000~5,000 万
```

### 5.2 渠道策略（Channels）

```
直销渠道（短期）：
  → 参加养老博览会（CAHE）、中国国际老龄产业博览会
  → 政府"智慧养老示范项目"申报，与民政局合作试点

渠道合作（中期）：
  → 与海尔（海尔智家）、小米（米家）等适老化产品生态合作
  → 接入京东养老频道、天猫超市老年频道

生态合作（长期）：
  → 与简单心理、壹心理联合运营专业内容
  → 与老年护理险打通健康管理服务
```

### 5.3 关键合作伙伴（Key Partners）

| 合作方类型 | 代表机构 | 合作内容 |
|---|---|---|
| 技术供应商 | Microsoft Azure | TTS + Viseme 语音服务 |
| 模型服务商 | 硅基流动（SiliconFlow） | DeepSeek LLM API |
| 数据合作方 | 清华大学 COAI 实验室 | PsyQA 心理数据集授权 |
| 内容合作方 | 北京心理危机研究与干预中心 | CBT 干预知识库专业验证 |
| 渠道合作方 | 各地养老服务企业 | 落地推广 |

---

## 六、项目管理计划

### 6.1 团队组织结构

```
项目组（5人）
├── 项目负责人（PM）      1人  统筹协调、对外沟通、进度管控
├── AI 算法工程师          1人  模型微调（ASR/SER）、评测指标、RAG
├── 后端工程师             1人  五层架构、WebSocket、API 集成
├── 前端工程师             1人  3D 数字人、适老化 UI、WebGL
└── 产品/运营              1人  需求分析、文档、演示脚本、竞品调研
```

### 6.2 项目阶段划分（4阶段，共10周）

#### Phase 1：技术选型与基础搭建（第 1~2 周）

| 任务 | 描述 | 负责人 | 工期 | 交付物 |
|---|---|---|---|---|
| T1.1 | 调研 ASR/SER/LLM/TTS 方案，确定选型 | AI 算法 | 3天 | 技术选型报告 |
| T1.2 | 搭建 FastAPI + WebSocket 骨架 | 后端 | 3天 | 可运行服务端框架 |
| T1.3 | 前端基础页面 + TalkingHead 3D 初始化 | 前端 | 3天 | 3D 数字人可渲染 |
| T1.4 | 收集竞品资料，制定用户调研问卷 | 产品 | 3天 | 竞品分析初稿 |
| T1.5 | 下载并验证基础模型（Whisper/BGE/DeepFace） | AI 算法 | 2天 | 各模型本地跑通 |

**里程碑 M1**（W2末）：基础框架通，可输出语音并驱动数字人嘴动

---

#### Phase 2：核心功能开发（第 3~6 周）

| 任务 | 描述 | 负责人 | 工期 | 交付物 |
|---|---|---|---|---|
| T2.1 | Whisper-Small LoRA 微调（中文增强） | AI 算法 | 7天 | 微调模型，CER < 10% |
| T2.2 | SER 模型集成，faceformer_adapter 开发 | AI 算法 | 5天 | 共情微表情 25FPS |
| T2.3 | LLM 流式接入 + 文本分块 + TTS 预取队列 | 后端 | 5天 | 流式对话，首音节 < 1.5s |
| T2.4 | Azure TTS + Viseme 口型同步集成 | 后端 | 3天 | 声画同步误差 < 50ms |
| T2.5 | DeepFace 摄像头情绪识别集成 | AI 算法 | 2天 | 视觉情绪实时传递 LLM |
| T2.6 | RAG 知识库构建（PsyQA + CBT） | AI 算法 | 3天 | ChromaDB 检索验证通过 |
| T2.7 | 打断/续播（Barge-in）机制开发 | 后端 | 4天 | 打断恢复 < 500ms |
| T2.8 | 多角色人设（小丽/老王/小明）配置 | 后端 | 2天 | 3 角色风格差异明显 |
| T2.9 | 前端适老化 UI（关怀模式、大字号） | 前端 | 3天 | care-mode 放大 25% |
| T2.10 | 提醒管理功能开发 | 后端 | 2天 | 自然语言设置提醒，到期播报 |

**里程碑 M2**（W6末）：完整对话闭环，全链路延迟 < 3s

---

#### Phase 3：测试、评测与优化（第 7~9 周）

| 任务 | 描述 | 负责人 | 工期 | 交付物 |
|---|---|---|---|---|
| T3.1 | 官方 25 维评测指标本地跑通 | AI 算法 | 4天 | prediction_emotion.npy 格式验证 |
| T3.2 | ASR 评测接口（POST /asr）联调 | 后端 | 1天 | 官方评测接口通过 |
| T3.3 | SER 嘈杂环境鲁棒性测试 | AI 算法 | 3天 | 噪声增强，回退策略验证 |
| T3.4 | 多用户并发 WebSocket 压力测试 | 后端 | 2天 | 5 并发延迟 < 4s |
| T3.5 | 用户可用性测试（5位老年用户） | 产品 | 3天 | 可用性报告，满意度 ≥ 4.0/5.0 |
| T3.6 | 前端性能优化（WebGL FPS ≥ 25） | 前端 | 2天 | Chrome/Edge 下 3D 流畅 |
| T3.7 | 安全性测试（心理危机响应） | 全员 | 2天 | 危机识别触发率 100% |

**里程碑 M3**（W9末）：评测指标达标，老年用户满意度达标

---

#### Phase 4：交付与部署（第 10 周）

| 任务 | 描述 | 负责人 | 工期 | 交付物 |
|---|---|---|---|---|
| T4.1 | Docker Compose 镜像打包 | 后端 | 2天 | docker-compose.yml + 镜像 tar |
| T4.2 | 提交文件整理 | PM | 2天 | 满足 500M 提交限制 |
| T4.3 | 演示视频录制与剪辑 | 产品 | 2天 | 3~5 分钟演示视频 |
| T4.4 | 答辩 PPT 制作 | PM + 产品 | 3天 | 专家答辩 PPT |
| T4.5 | 百度网盘模型文件上传 | 全员 | 1天 | 网盘链接有效性确认 |

**里程碑 M4**（W10末）：材料全部提交，Docker 镜像可一键复现

---

### 6.3 甘特图

```
Week:    W1   W2   W3   W4   W5   W6   W7   W8   W9   W10
Phase 1: ████ ████
  T1.1   ███
  T1.2   ████
  T1.3        ████
  T1.4   ████
  T1.5   ██

Phase 2:      ████ ████ ████ ████
  T2.1        ████████
  T2.2              ████████
  T2.3        ████████
  T2.4              ████
  T2.5        ████
  T2.6              ████
  T2.7                    ████████
  T2.8                    ████
  T2.9              ████████
  T2.10                       ████

Phase 3:                          ████ ████ ████
  T3.1                          ████████
  T3.2                          ██
  T3.3                                ████████
  T3.4                          ████
  T3.5                                ████████
  T3.6                                      ████
  T3.7                                      ████

Phase 4:                                         ████

里程碑:   M1              M2              M3         M4
          ↑               ↑               ↑           ↑
         W2末            W6末            W9末        W10末
```

### 6.4 资源配置计划

**人力投入比例**：

| 角色 | Phase 1 | Phase 2 | Phase 3 | Phase 4 |
|---|---|---|---|---|
| 项目负责人 | 20% | 30% | 30% | 50% |
| AI 算法工程师 | 80% | 100% | 100% | 20% |
| 后端工程师 | 80% | 100% | 80% | 30% |
| 前端工程师 | 80% | 80% | 60% | 20% |
| 产品/运营 | 60% | 40% | 60% | 80% |

**计算资源**：

| 资源 | 用途 | 配置 |
|---|---|---|
| 开发工作站 | 模型训练 + 本地运行 | i7-12700 + RTX 3060 12G + 32GB RAM |
| 云服务器（可选） | CI/CD + 远程演示 | 4核8G 实例，按需开启 |
| Azure 账号 | TTS + Viseme 服务 | 标准套餐，月赠 500,000 字符免费额度 |
| SiliconFlow 账号 | DeepSeek LLM API | 按 token 计费，新用户有免费额度 |

---

## 七、可行性分析

### 7.1 技术可行性

| 技术组件 | 成熟度 | 验证状态 |
|---|---|---|
| Whisper-Small + LoRA 微调 | 高 | ✅ 本地训练完成，/asr 接口官方验证通过 |
| DeepFace 情绪识别 | 高 | ✅ 已集成，1s 限频正常运行，CPU推理 < 200ms |
| HuBERT 中文 SER | 中高 | ✅ 已集成，25FPS 帧序列格式正确，~76% 准确率 |
| DeepSeek-V3.2（SiliconFlow）| 高 | ✅ 已接通，流式首 token < 1s |
| Azure Neural TTS + Viseme | 高 | ✅ 已集成，口型同步测试通过，延迟 < 400ms |
| ChromaDB + BGE-small-zh | 高 | ✅ 知识库构建完成，检索 < 50ms |
| TalkingHead 3D 渲染 | 中 | ✅ Chrome/Edge 正常，Firefox 需额外适配 |

**关键指标验证**：
```
✅ 端到端延迟：0.8~1.2 s（目标 < 2 s）
✅ 打断恢复时间：< 300 ms（目标 < 500 ms）
✅ 25 维评测输出格式：[N, K, T, 25] 验证通过
✅ ASR 评测接口：POST /asr 返回格式正确
✅ Docker 一键部署：docker compose up 完整运行
```

### 7.2 经济可行性

**API 运营成本**（10,000 次对话/月）：

| 成本项 | 月支出 |
|---|---|
| SiliconFlow LLM（8M tokens，¥2/M） | ¥16 |
| Azure TTS（80万字符，¥15/100万字符） | ¥12 |
| 云服务器（管理端） | ¥300~500 |
| **合计** | **¥328~528/月** |

**每终端毛利分析**（机构版 ¥800/月/终端，1,000 次对话/月/终端）：

```
营收：¥800    变动成本：¥33    毛利：¥767（毛利率 ≈ 96%）
扣除人力分摊（¥200）→ 净利润约 ¥567/月/终端
```

**一次性研发投入**：

| 工作项 | 工时 | 小计 |
|---|---|---|
| ASR 微调（数据标注 + 训练） | 40 h | ¥4,000 |
| SER 模型集成 + 调优 | 20 h | ¥2,000 |
| 五层架构后端开发 | 80 h | ¥8,000 |
| 前端 3D 数字人集成 | 40 h | ¥4,000 |
| 测试与联调 | 30 h | ¥2,400 |
| **合计** | **210 h** | **≈ ¥20,400** |

**投资回收期**：签约 3 个终端 × ¥567 净利/月 = ¥1,701/月，约 **12 个月**回本。

### 7.3 社会效益

| 效益维度 | 可量化指标 |
|---|---|
| 孤独感改善 | AI 陪伴可使老年人 UCLA 孤独感评分降低约 18%（已有研究支撑） |
| 心理干预覆盖率 | 单台设备可服务 50~100 名老人，替代 1~2 名专职心理关怀岗位 |
| 家属安心指数 | 情绪状态日报，子女远程了解父母情感状态 |
| 政策符合度 | 满足《养老机构等级划分》心理关怀服务指标，助力机构星级提升 |

---

## 八、风险评估与对策

### 8.1 技术风险矩阵

| 风险项 | 概率 | 影响 | 等级 | 当前状态 | 对策 |
|---|---|---|---|---|---|
| Azure TTS 服务超时/宕机 | 中 | 高 | **高** | ⚠️ 已知风险 | 30s 超时熔断 + `text_only` 降级；备用 edge-tts |
| SER 模型噪声环境准确率下降 | 高 | 中 | **高** | ⚠️ 持续优化 | `_fallback()` 纯能量回退；DeepFace 视觉通道补偿 |
| Whisper LoRA 口音/方言泛化差 | 中 | 中 | **中** | ✅ 已缓解 | 数据增强（混响+噪声叠加）；识别失败不崩溃 |
| DeepSeek API 限流或 Key 失效 | 低 | 高 | **中** | ✅ 已预案 | 备用 API Key；可降级至 Qwen2.5-7B 本地推理 |
| RAG 知识库为空 | 低 | 低 | **低** | ✅ 已处理 | `try/except` 降级，LLM 自身知识兜底，不中断对话 |
| WebGL 兼容性（老年平板） | 中 | 中 | **中** | ⚠️ 需测试 | 降级为 2D 卡通形象；关怀模式简化界面 |

### 8.2 市场风险

| 风险项 | 对策 |
|---|---|
| 养老机构采购决策周期长（6~12个月） | 以政府试点项目为突破口，缩短决策链 |
| 大厂（阿里/华为）进入同类赛道 | 专注"情感陪护+心理干预"垂直定位，而非通用养老硬件 |
| 老人不接受 AI 陪伴（信任问题） | 以"数字孙辈/数字老友"形象定位，非"机器人"概念 |

### 8.3 伦理与安全风险

| 风险项 | 对策 |
|---|---|
| 用户误以为数字人是真实人类 | 开场白明确"我是AI数字人"；不主动伪装人类 |
| 识别到自杀/自残风险 | LLM 约束：温和建议专业帮助，不提供方法；规划触发紧急联系人通知 |
| 对话内容隐私泄露 | 全本地推理；session_memory.json 仅存本地；零上传 |
| 老人过度依赖AI减少真实社交 | 每日对话时长提醒；主动引导用户与家人联系 |
| 情绪识别偏差导致错误回应 | 双通道（视觉+声学）互补；情绪仅作语气参考，不改变核心干预逻辑 |

### 8.4 运营风险

| 风险项 | 对策 |
|---|---|
| API 费用超支 | 会话历史限 6 条；max_tokens=2048；工具调用仅触发天气关键词 |
| 并发连接导致服务崩溃 | FastAPI + uvicorn 全异步架构；每 WebSocket 独立 asyncio Task；SER/DeepFace 线程池隔离 |
| 模型文件丢失/损坏 | 百度网盘双备份（镜像 + 模型文件）；Docker 镜像打包保留完整依赖 |

---

*文档版本：v2.0（合并版）| 编写日期：2026-05 | 项目组：Digital-Human-Companion Team*
